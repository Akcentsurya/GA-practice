name: myworkflow

on: 
  push:

jobs:
  test:
     runs-on: ubuntu-latest

     steps: 
       - name: System Info
         run: |
          cat /etc/os-release
          ls
          ls -R
          ls -lrt
          ls -la
     
