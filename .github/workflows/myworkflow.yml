name: myworkflow

on: 
  push:

jobs:
  test:
     runs-on: ubuntu-latest

     steps: 
       - name: System Info
         run: |
         
          pwd
          touch app.txt
          ls
          echo "this is brahma">app.txt
          cat app.txt
          
     
