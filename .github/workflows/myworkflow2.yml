name: multiplesteps-workflow

on: 
  push:

jobs:
  build:
     runs-on: ubuntu-22.04

     steps:
       - name: step1
         run: echo"multiple steps"

       - name: step2
         run: pwd

       - name: step3
         run: touch app.txt

       - name: step4
         run: echo" this is multiple steps practice"
          
        
